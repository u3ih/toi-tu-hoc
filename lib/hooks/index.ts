/**
 * Reusable React hooks with no knowledge of this site's content.
 *
 * Behaviour only — anything that renders belongs in `lib/ui/` (presentation
 * primitives) or `components/` (this site's own pieces).
 */
export { useBodyScrollLock } from './use-body-scroll-lock'
